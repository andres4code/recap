package com.sda.tallinn6.advancedFeatures.optionals;
/*
import com.sda.tallinn6.advancedFeatures.encapsulation.Student;

import java.util.Optional;

public class optionalsDemo {
    public static void main(String[] args) {
        String name = "Hello";
        Optional<String> optionalString = Optional.ofNullable(name);//to check if reference is null or not
        Optional<Student> studentOptional = Optional.ofNullable(null);

        if (optionalString.isPresent()){
            System.out.println(" is present");
            System.out.println(name.length());
        }
        if (studentOptional.isPresent()){
            System.out.println("is present");
        }
    }
}

 */
